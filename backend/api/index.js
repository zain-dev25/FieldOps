// Vercel Serverless Entry Point
// This file exports the Express app for Vercel's serverless environment.
// server.js is used for local development (it calls app.listen).

import app from '../server.js';

export default app;
